version: "3.9"
services:
  n8n:
    ports:
      - "5678:5678"
    environment:
      N8N_PROTOCOL: http
      WEBHOOK_URL: http://localhost:5678/
  traefik:
    # لا حاجة لفتح المنافذ محلياً إن لم تستخدمه
    ports: []
